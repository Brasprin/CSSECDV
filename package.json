{
  "name": "cssecdv",
  "version": "1.0.0",
  "description": "CSSECDV - Security Development Project",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server": "npm start --prefix backend",
    "server:dev": "npm run dev --prefix backend",
    "client": "npm run build --prefix frontend && npm run preview --prefix frontend",
    "client:dev": "npm run dev --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
